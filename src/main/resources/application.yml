spring:
  application:
    name: hr-workflow

  datasource:
    url: jdbc:postgresql://postgres:5432/hr_workflow_db
    username: admin
    password: Livefree1128*

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never

server:
  port: 8080
